!!!
html
  head
    title What size is my browser?
    meta(name="viewport",content="width=device-width")
    link(rel='stylesheet',type='text/css',href='//fonts.googleapis.com/css?family=Noto+Sans')
    link(rel="stylesheet",type="text/css",href="index.css")
    link(rel="stylesheet",type="text/css",href="app.css")

    //- If we are in development
    if (config.env === 'development')
      // DEV: Watch files for changes
      script(src="https://raw.github.com/twolfson/File-Watcher/master/src/watcher.js")
      script(src="https://raw.github.com/twolfson/Resource-Collector/master/src/collector.js")
      script
          (function () {
             var watcher = new FileWatcher(),
                 resources = ResourceCollector.collect();
             watcher.addListener(function () {
               location.reload();
             });
             watcher.watch(resources);
          }());
    else
      //- Otherwise, if we are in production
      script
        //- Load in Google Analytics and record a pageview
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-39787166-1');
        ga('send', 'pageview');

    //- Nice touch for developers
    script
      if (window.console) {
        console.log('If you are looking for the source, it is all minified (except for this).')
        console.log('You are better off reading the original at http://github.com/twolfson/whatsizeismybrowser');
      }

  body
    .container
      .header Your browser is
      .dimensions.grid.grid-center
        .width.grid-cell.size1of6
          .value
            span.w px
          .hint (width)
        .times.grid-cell.size1of6 &times;
        .height.grid-cell.size1of6
          .value
            span.h px
          .hint (height)

      p <strong>Fun fact</strong>: This hands out <strong>printer dimensions</strong> in a print preview.

      p
      | Inspired by <a href="http://matanich.com/test/viewport-width/">viewportSize</a> and <a href="http://www.slideshare.net/x00mario/stealing-the-pie">Stealing the Pie</a>.
      | Written by <a href="http://twolfson.com/projects">Todd Wolfson</a>.

    //- GitHub ribbon -- https://github.com/blog/273-github-ribbons
    a(href="//github.com/twolfson/whatsizeismybrowser")
      img.github-ribbon(src="//s3.amazonaws.com/github/ribbons/forkme_right_orange_ff7600.png", alt="Fork me on GitHub")


    //- DEV: I wanted to initially do some modulo combination but that is not possible
    //- and doing things on a per-digit basis winds up forcing 9999 for the ones case alone.
    //- The tens is 999 (e.g. min-height: 10px and max-height: 20px). As a result, it is just silly.

    //- TODO: Can we short circuit the cascade via nested media queries? (e.g. @media (min-width: 1px) { content: "1"; @media (min-width: 2px) { content: "2"; } })
    //- TODO: Another perf test: Create 4000 digits for each (i.e. 0000 - 9999) and toggle each via `display: none;`. It might be faster than dynamic content (doubt it)
    //- TODO: In this case, we are intended to chain them together
    // TODO: Choose a good looking font ()
    // TODO: Can we half the size of index.css with `or` statements? (and is that performant?)
    // TODO: Can we half reduce the size of index.css with commas? (this will remove the `content: "1";` parts)
    // TODO: Favicon me
    //- TODO: I would love a favicon that displays browser resolution
    //- TODO: Add testing (via testling/phantomjs/casperjs) -- something that will work
    //- TODO: SEO terms
    //- TODO: Determine magical font sizings
    //- TODO: Hide github ribbon on print preview
    //- TODO: Need some vertical padding after values
    //- TODO: Need some more vertical padding after hints